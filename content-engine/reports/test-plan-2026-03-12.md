# Kiuli End-to-End Test Plan — 2026-03-12

## Goal

Verify the complete pipeline: scrape → cascade → review → enhance → publish → public page. Find every bug. Fix them. Get to a state where M3 frontend work can begin against real populated content.

---

## Prerequisites

- Admin access: Confirmed (logged in via browser)
- API keys: SCRAPER_API_KEY and CONTENT_SYSTEM_SECRET confirmed
- DB access: Confirmed via db_query/db_exec tools
- Browser: Chrome with Claude in Chrome, can navigate admin and public pages

---

## Phase 0: Clean Slate Reset

**Action:** Execute clean-slate-reset.sql via db_exec
**Verification:**
- Count query: all 14 tables return 0 rows
- Admin UI: Itineraries list empty, Content Engine dashboard empty, Destinations empty, Properties empty, Posts empty, Media empty

---

## Phase 1: Scrape One Itinerary

**Action:** Trigger scrape of ONE sample itinerary (Tanzania Big Game, ID will be new)
```
POST https://kiuli.com/api/scrape-itinerary
Authorization: Bearer {SCRAPER_API_KEY}
Body: { "url": "https://itrvl.com/client/portal/LrYsHSgMbxBrZeKDuESwZreUIrTP38pDBttfy9GrsXoDsnvS3ZI096AFr5UOzUL4/680df39120a6c6005b2bfc1e" }
```

**Verification checklist (each is a specific DB query or UI check):**

### 1.1 Job Created
- Query: `SELECT id, status, current_phase FROM itinerary_jobs ORDER BY id DESC LIMIT 1`
- Expected: status='processing' initially, then 'completed'

### 1.2 Itinerary Created with V7 Fields
- Query: `SELECT id, title, title_itrvl, title_enhanced, _status FROM itineraries ORDER BY id DESC LIMIT 1`
- Expected: title populated, title_itrvl populated, title_enhanced NULL, _status='draft'
- Query: Check days exist: `SELECT count(*) FROM itineraries_days WHERE _parent_id = {id}`
- Query: Check segments exist: `SELECT count(*) FROM itineraries_blocks_stay WHERE _parent_id = {id}` (and activity, transfer)
- Query: Check FAQ items: `SELECT count(*) FROM itineraries_faq_items WHERE _parent_id = {id}`
- Query: Check overview: `SELECT count(*) FROM itineraries_overview_highlights WHERE _parent_id = {id}`

### 1.3 Images Processed
- Query: `SELECT count(*), status FROM image_statuses WHERE job_id = {jobId} GROUP BY status`
- Expected: All 'complete' or 'skipped', zero 'failed'
- Query: `SELECT count(*) FROM media WHERE id IN (SELECT media_id FROM image_statuses WHERE job_id = {jobId} AND media_id IS NOT NULL)`
- Expected: Media records created with imgix_url populated

### 1.4 Hero Image Selected
- Query: `SELECT hero_image_id, hero_video_id FROM itineraries WHERE id = {id}`
- Expected: hero_image_id NOT NULL
- Admin UI: Navigate to itinerary, verify hero image displays

### 1.5 Segment Images Linked
- Query: Check itineraries_rels for media relationships
- Admin UI: Open itinerary, check segments have images in gallery

### 1.6 Knowledge Base Populated
- Query: `SELECT count(*) FROM properties` — should be >0
- Query: `SELECT count(*) FROM destinations` — should be >0
- Query: `SELECT count(*) FROM transfer_routes` — should be >0
- Query: `SELECT count(*) FROM itinerary_patterns` — should be 1
- Query: Properties have destination_id set, external_ids populated
- Query: Transfer routes have from/to destinations and observations

### 1.7 Schema Generated
- Query: `SELECT schema_status, publish_checklist FROM itineraries WHERE id = {id}`
- Expected: schema_status='pass' or 'warn', publish_checklist populated

### 1.8 Notification Created
- Query: `SELECT count(*) FROM notifications WHERE itinerary_id = {id}`
- Expected: ≥1

---

## Phase 2: Verify Cascade Triggered

The triggerCascade hook fires on itinerary CREATE. Check it ran.

### 2.1 Content Job Created
- Query: `SELECT id, job_type, status FROM content_jobs WHERE job_type = 'cascade' ORDER BY id DESC LIMIT 1`
- Expected: status='completed'

### 2.2 ContentProjects Created
- Query: `SELECT content_type, count(*) FROM content_projects GROUP BY content_type`
- Expected: destination_page and property_page entries exist
- Query: All have stage='idea', origin_pathway='cascade'

### 2.3 Destinations Resolved
- Query: `SELECT id, name, type, _status FROM destinations ORDER BY name`
- Expected: Countries (Tanzania) and destinations (Serengeti, Ngorongoro, etc.) created

### 2.4 Properties Resolved
- Query: `SELECT id, name, destination_id, _status FROM properties ORDER BY name`
- Expected: Properties created and linked to destinations

### 2.5 Relationships Set
- Query: `SELECT count(*) FROM itineraries_rels WHERE parent_id = {itinId} AND destinations_id IS NOT NULL`
- Query: `SELECT count(*) FROM destinations_rels WHERE properties_id IS NOT NULL`
- Expected: Itinerary linked to destinations, destinations linked to properties

### 2.6 Decompose Triggered
- Query: `SELECT count(*) FROM content_projects WHERE origin_pathway = 'decompose'`
- Expected: Article candidates generated (>0)

---

## Phase 3: Admin UI Verification

Navigate each custom view and verify rendering.

### 3.1 Import Itinerary Page
- Navigate: Click "Import Itinerary" in sidebar
- Check: Page renders, form accepts iTrvl URL, shows job status

### 3.2 Itinerary Editor (V7)
- Navigate: Open the scraped itinerary
- Check: Hero image displays, V7 FieldPairEditors render for title/overview/segments
- Check: Day-by-day view shows segments with images
- Check: FAQ section renders
- Check: Investment level section renders
- Check: Publish checklist visible

### 3.3 Content Engine Dashboard
- Navigate: Click "Content Engine" in sidebar
- Check: Dashboard renders, shows ContentProjects
- Check: Filters work (by stage, type)
- Check: Can click into a project

### 3.4 Content Project Workspace
- Navigate: Click into a destination_page project
- Check: Workspace renders
- Check: Conversation panel works
- Check: Research/draft/publish actions visible

### 3.5 Image Library
- Navigate: Click "Image Library" in sidebar
- Check: Images display with thumbnails
- Check: Filtering works
- Check: Image detail shows AI enrichment data

### 3.6 Destinations Collection
- Navigate: Click "Destinations" in sidebar
- Check: List shows created destinations
- Check: Click into one — detail page renders

### 3.7 Properties (via itinerary stays)
- Check: Properties accessible somehow in admin
- Check: Detail page renders with fields

---

## Phase 4: Content Engine Draft Test

### 4.1 Draft a Destination Page
- Action: Call `/api/content/draft` for one destination_page ContentProject
```
POST https://kiuli.com/api/content/draft
Authorization: Bearer {CONTENT_SYSTEM_SECRET}
Body: { "projectId": {id} }
```
- Check: Project stage moves from 'idea' to 'draft'
- Check: Draft content populated in project fields
- Check: Content quality (not hallucinated, based on scraped data)

### 4.2 Draft a Property Page
- Same as 4.1 but for a property_page project
- Verify property-specific content generated

### 4.3 Review Draft in Workspace
- Admin UI: Open project workspace
- Check: Draft content visible
- Check: Conversation shows drafting history

---

## Phase 5: Publish Test

### 5.1 Publish Destination
- Action: Call `/api/content/publish` for the drafted destination project
- Check: Destination record updated with content
- Check: _status changed to 'published'
- Check: Content matches draft

### 5.2 Publish Property
- Same for property

### 5.3 Publish an Article (if ready)
- Find an article candidate, draft it, publish it
- Check: Post created with hero image, content, meta fields

---

## Phase 6: Public Page Verification

### 6.1 Homepage
- Browse: https://kiuli.com/
- Check: Renders without errors

### 6.2 Safaris Listing
- Browse: https://kiuli.com/safaris
- Check: Shows scraped itinerary (NOTE: currently shows drafts — Gap 7)

### 6.3 Itinerary Detail
- Browse: https://kiuli.com/safaris/{slug}
- Check: Hero image, trip overview, journey narrative, investment level, FAQ all render
- Check: Segment images display

### 6.4 Destinations Listing
- Browse: https://kiuli.com/destinations
- Check: Published destinations appear

### 6.5 Destination Detail
- Browse: https://kiuli.com/destinations/{slug}
- Check: Content from published destination renders

### 6.6 Properties Listing
- Browse: https://kiuli.com/properties
- Check: Published properties appear

### 6.7 Property Detail
- Browse: https://kiuli.com/properties/{slug}
- Check: Content renders

### 6.8 Articles Listing
- Browse: https://kiuli.com/articles
- Check: Published articles with hero images appear

---

## Phase 7: Scrape Remaining 4 Samples

After Phase 1-6 confirms the pipeline works for ONE itinerary:

### 7.1 Scrape Each Remaining Sample
- Kenya Honeymoon
- South Africa/Mozambique
- Rwanda Primate
- Kenya Family

### 7.2 Verify Cascade for Each
- ContentProjects created for new destinations/properties
- No duplicates (cascade is idempotent)
- Knowledge base accumulates (properties gain observationCount, transfer routes gain observations)

### 7.3 Cross-Itinerary Verification
- Properties shared across itineraries linked correctly
- Destinations deduplicated
- Transfer routes accumulate observations

---

## Phase 8: Full Content Production Run

### 8.1 Batch Draft All ContentProjects
- Draft all destination_page projects
- Draft all property_page projects
- Review quality of each

### 8.2 Publish All
- Publish all destinations
- Publish all properties

### 8.3 Article Candidates
- Review decompose output
- Select candidates for drafting
- Draft, review, publish selected articles

### 8.4 Final Public Site Review
- All pages populated with real content
- No broken images, no placeholder text
- Navigation works between all page types

---

## Bug Tracking

Every bug found during testing gets logged here with:
- Phase/Step where found
- Description
- Severity (blocker / major / minor)
- Fix applied
- Verification after fix

---

## Graham's Involvement Required

- Phase 0: None (I execute reset)
- Phase 1-2: None (I trigger scrape and verify via DB + API)
- Phase 3: None (I navigate admin UI via browser)
- Phase 4-5: None (I call API endpoints)
- Phase 6: None (I browse public pages)
- Phase 7: None (I trigger remaining scrapes)
- Phase 8: **Graham reviews article candidates and selects which to draft** — this is the only human editorial judgment needed

Total Graham time required: ~30 minutes for article selection in Phase 8.

---

*Generated 2026-03-12*
